import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miApp2132830.app',
  appName: 'miApp2132830',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
